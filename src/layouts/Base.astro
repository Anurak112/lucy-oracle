---
import { ClientRouter } from 'astro:transitions';
import Cosmos from '../components/Cosmos.astro';
import { meta } from '../data/site';

interface Props {
  title?: string;
  description?: string;
}

const { title = `${meta.name} — ${meta.role}`, description = meta.description } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site).href;
// BASE_URL may or may not carry a trailing slash depending on trailingSlash config —
// normalize so public-asset URLs join correctly (avoids e.g. "/lucy-oracleog.png").
const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const ogImage = new URL(`${base}og.png`, Astro.site).href;

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Lucy (ลูซี่)',
  alternateName: 'ลูซี่',
  description: meta.description,
  url: canonical,
  jobTitle: 'AI Oracle & creative chief-of-staff',
  knowsLanguage: ['th', 'en'],
  sameAs: [meta.repo],
};
---

<!doctype html>
<html lang="th">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta name="theme-color" content="#0D0B09" />

    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="profile" />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="ลูซี่ · Lucy — Oracle of คุณนุขา, the storyteller of Siam" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImage} />

    <link rel="icon" href={`${base}favicon.svg`} type="image/svg+xml" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Serif+Thai:wght@500;700;800&family=IBM+Plex+Sans+Thai:wght@400;500;600&family=JetBrains+Mono:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
    <ClientRouter />
  </head>
  <body>
    <div id="intro">
      <video id="intro-vid" src={`${base}studio-intro.mp4`} muted playsinline autoplay preload="auto"></video>
      <button id="intro-skip" type="button" aria-label="ข้าม intro">ข้าม&nbsp;›</button>
    </div>
    <Cosmos />
    <slot />
    <script is:inline>
      (function () {
        var el = document.getElementById('intro');
        if (!el) return;
        var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        var seen = false;
        try { seen = sessionStorage.getItem('psi-intro') === '1'; } catch (e) {}
        if (reduce || seen) { if (el.parentNode) el.parentNode.removeChild(el); return; }
        var done = false;
        function dismiss() {
          if (done) return; done = true;
          try { sessionStorage.setItem('psi-intro', '1'); } catch (e) {}
          el.classList.add('gone');
          setTimeout(function () { if (el.parentNode) el.parentNode.removeChild(el); }, 850);
        }
        var v = document.getElementById('intro-vid');
        var skip = document.getElementById('intro-skip');
        if (skip) skip.addEventListener('click', dismiss);
        if (v) {
          v.addEventListener('ended', dismiss);
          v.addEventListener('error', function () { if (el.parentNode) el.parentNode.removeChild(el); });
          var p = v.play && v.play();
          if (p && p.catch) p.catch(function () {});
        }
        setTimeout(dismiss, 7500);
      })();
    </script>
  </body>
</html>

<style is:global>
  :root {
    /* warm-black observatory — the manus "Siamshort Method" family */
    --bg-0: #0d0b09;
    --bg-1: #15100d;
    --card: #171210;
    --card-hover: #1e1712;
    --ink: #e5dac7;
    --text: var(--ink);
    --muted: #a59585;
    --dim: var(--muted);
    --faint: #6c6158;
    --gold: #e49e22;
    --gold-2: #f0b84b;
    --gold-soft: rgba(228, 158, 34, 0.1);
    --red: #c53637;
    --coral: #db665a;
    --line: rgba(228, 158, 34, 0.16);
    --line-soft: rgba(228, 158, 34, 0.08);
    --border: var(--line-soft);
    --sans: 'IBM Plex Sans Thai', 'Leelawadee UI', 'Sarabun', system-ui, sans-serif;
    --serif: 'Noto Serif Thai', 'Sarabun', Georgia, serif;
    --mono: 'JetBrains Mono', 'Consolas', ui-monospace, monospace;
  }

  * {
    box-sizing: border-box;
  }
  html {
    scroll-behavior: smooth;
  }
  body {
    margin: 0;
    background: var(--bg-0);
    background-image: radial-gradient(120% 70% at 50% -8%, rgba(228, 158, 34, 0.06), transparent 55%);
    color: var(--ink);
    font-family: var(--sans);
    font-size: 16px;
    line-height: 1.7;
    -webkit-font-smoothing: antialiased;
  }
  a {
    color: var(--gold);
    text-decoration: none;
  }
  a:hover {
    text-decoration: underline;
  }
  :focus-visible {
    outline: 2px solid var(--gold);
    outline-offset: 3px;
    border-radius: 4px;
  }
  .wrap {
    max-width: 1020px;
    margin: 0 auto;
    padding: 0 22px;
    position: relative;
    z-index: 1;
  }
  .mono {
    font-family: var(--mono);
  }

  /* ── one-time studio-intro (HyperFrames MP4) ── */
  #intro {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: #0d0b09;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 1;
    transition: opacity 0.85s ease;
  }
  #intro.gone {
    opacity: 0;
    pointer-events: none;
  }
  #intro video {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  #intro-skip {
    position: absolute;
    bottom: 26px;
    right: 26px;
    font-family: var(--mono);
    font-size: 12px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--muted);
    background: rgba(13, 11, 9, 0.5);
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 9px 16px;
    cursor: pointer;
    transition: color 0.2s, border-color 0.2s;
  }
  #intro-skip:hover {
    color: var(--gold);
    border-color: var(--gold);
  }
  @media (max-width: 760px) {
    #intro video { object-fit: contain; }
  }
  @media (prefers-reduced-motion: reduce) {
    #intro { display: none !important; }
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
  }

  /* ── section grammar (kicker → serif head → lead → content → plate) ── */
  section {
    padding: 58px 0 30px;
    border-top: 1px solid var(--line-soft);
  }
  .scene-tab {
    display: inline-block;
    background: var(--red);
    color: #f7efe0;
    font-family: var(--mono);
    font-size: 10.5px;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    padding: 4px 10px;
    border-radius: 5px;
  }
  .kicker {
    font-family: var(--mono);
    font-size: 10.5px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--faint);
    margin: 14px 0 6px;
  }
  section h2 {
    font-family: var(--serif);
    font-weight: 800;
    font-size: clamp(26px, 4.2vw, 38px);
    line-height: 1.12;
    margin: 0;
    color: var(--ink);
    text-wrap: balance;
  }
  .lead {
    color: var(--muted);
    max-width: 62ch;
    margin: 12px 0 0;
    font-size: 15.5px;
  }
  .lead b {
    color: var(--ink);
    font-weight: 600;
  }
  .plate {
    text-align: center;
    margin-top: 16px;
    font-size: 10px;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    color: var(--faint);
  }
  .plate b {
    color: var(--gold);
    font-weight: 400;
  }

  .grid {
    display: grid;
    gap: 14px;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    margin-top: 26px;
  }
  .card {
    background: rgba(23, 18, 16, 0.72);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid var(--line-soft);
    border-radius: 14px;
    padding: 20px 22px;
    transition: border-color 0.18s, background 0.18s;
  }
  .card:hover {
    border-color: var(--line);
    background: rgba(30, 23, 18, 0.82);
  }
  .card h3 {
    font-size: 15.5px;
    font-weight: 600;
    margin: 10px 0 5px;
    color: var(--ink);
  }
  .card p {
    font-size: 13.5px;
    line-height: 1.65;
    color: var(--muted);
    margin: 0;
  }
  .num {
    font-family: var(--mono);
    font-size: 15px;
    color: var(--gold);
  }
  .label {
    font-family: var(--mono);
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--gold);
  }

  /* reveal-on-scroll — degrades gracefully without JS / reduced motion */
  @media (prefers-reduced-motion: no-preference) {
    .reveal {
      opacity: 0;
      transform: translateY(14px);
      transition: opacity 0.55s ease, transform 0.55s ease;
    }
    .reveal.in {
      opacity: 1;
      transform: none;
    }
  }
</style>
