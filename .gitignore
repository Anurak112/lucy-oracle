# build output
dist/
# dependencies
node_modules/
# generated types
.astro/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment
.env
.env.production
# macOS / editor
.DS_Store

# --- Lucy's brain: never commit into this PUBLIC repo ---
# ψ/  = junction to the PRIVATE lucy-vault repo (the live brain)
# psi/ = frozen latin archive (203 untracked files must not leak)
# The 2 soul files + starter-kit gitlink are already tracked → stay public
# (gitignore never affects already-tracked paths)
/ψ/
/psi/
